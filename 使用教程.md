# ttdownload-web 完整使用教程

> 面向两类人：**① 服务器管理员**（部署/运维服务端）　**② 使用者**（用网页下载 + 用安卓 App 消费）
> 阅读顺序建议：第一部分 → 第二部分 → 第三部分 → 第四部分。

---

# 第一部分　服务器端：一键部署

## 1.1 环境要求

- Ubuntu **20.04 / 22.04 / 24.04**（推荐）；**Debian 12/13、树莓派 OS（Raspberry Pi OS）也可以用**
  （已实测树莓派 4B + 树莓派 OS：aria2 / transmission / yt-dlp / ffmpeg 全部自动装好）
- Ubuntu 18.04 也能跑（脚本自动改用 Node 18），但系统 Python 太旧，**公开视频（yt-dlp）模块可能不可用**
- Node.js 由脚本安装：**目标是 Node 20**；仅 Ubuntu 18.04 这类 glibc 过老的系统才退回 Node 18
  （`./deploy.sh --check-deps` 会显示当前版本与将要执行的动作）
- 一枚可以 `sudo` 的账号（脚本要装系统包、注册 systemd 服务）
- 系统使用 systemd（Ubuntu Server 默认满足）
- 一个**足够大的数据盘**（默认要求保留 10GiB 空闲）
- 能访问外网（npm 依赖、apt 源、NodeSource、GitHub）
- 磁盘 ≈2GB 给 npm 依赖与构建产物；服务器若开了 ufw，脚本会自动放行 Web 端口

> 一条命令什么都装好了吗？**是的**——Node/npm、aria2、transmission、yt-dlp、ffmpeg、
> openssl、zip/unzip、jq、编译工具链都由脚本负责，你只需要在执行过程中
> **回答 transmission 安装脚本的交互提问**（白名单、RPC 密码）。

## 1.2 部署（一条命令）

把工程目录拷到服务器（git clone / scp / 解压 zip 都行），然后：

```bash
git clone git@github.com:markternu/ttdownload-web.git
cd ttdownload-web
sudo ./deploy.sh
```

脚本会自动：

1. `apt-get install` 安装基础依赖：`curl ca-certificates gnupg openssl zip unzip ffmpeg jq
   build-essential python3`（`build-essential/python3` 用于 better-sqlite3 没有预编译包时的本机编译），
   并安装 **Node.js**（没有或低于 18 时用 NodeSource 装 20.x；Ubuntu 18.04 自动改用 18.x）、
   **yt-dlp**（优先 pip，Ubuntu 24.04 自动加 `--break-system-packages`，失败则 apt / 官方二进制）；
   **aria2 与 transmission 单独判断**：
   - **aria2**：已安装则跳过；未安装则执行 `apt install -y aria2`；
   - **transmission**：已安装（`transmission-daemon` 存在）则跳过、**不做任何改动**；未安装则**必须**运行工程自带的
     `deploy/ubuntutr.sh`（就是你的 Transmission 终极安装配置脚本 v4.0），
     **脚本里的交互提问原样保留**：是否启用 IP 白名单 →（可选）使用检测到的 SSH IP 或手动输入网段 →
     设置 RPC 登录密码（两次确认）→ 自动完成卸载旧版/安装/写配置/systemd/启动/自检；
     安装完成后 `deploy.sh` 会再问你一次「刚才设置的 RPC 密码」并写入 `.env`（用于 BT 模块调用 RPC）；
     如果跳过不填，也可以稍后在网页「设置 → 网络设置 → transmission RPC」里填（用户名默认 `opengl`）。
2. 创建下载目录树 `/ttdownload/...`
3. 安装 npm 依赖，构建前端（`public/`）与后端（`dist/`）
4. 生成 `.env`（含随机安卓 Token；**已存在则不覆盖**）
5. 如启用了 ufw，自动放行 Web 端口（aria2/transmission 的 RPC 只监听本机，无需对外）
6. 注册并启动 systemd 服务 `ttdownload-web`
7. 健康检查，并打印访问地址和安卓 Token

成功后终端会打印类似：

```
============================== 部署成功 ==============================
Web 管理界面 : http://192.168.1.10:8080/
API 健康检查 : http://192.168.1.10:8080/api/health
安卓 App 填写 : 服务器地址 http://192.168.1.10:8080   Token 3f2a...
下载根目录   : /ttdownload
服务管理     : systemctl status|restart|stop ttdownload-web
====================================================================
```

## 1.3 常用参数

```bash
sudo ./deploy.sh --port 9000          # 换端口
sudo ./deploy.sh --root /data/dl      # 换下载根目录
sudo ./deploy.sh --no-apt             # 依赖已装好，跳过 apt
sudo ./deploy.sh --skip-web           # 跳过前端构建（已有 public/）
sudo ./deploy.sh --update             # 【已部署过】拉取最新代码 + 重新构建 + 重启（最常用）
sudo ./deploy.sh --status             # 查看服务与健康状态
sudo ./deploy.sh --restart            # 重启服务
sudo ./deploy.sh --stop               # 停止服务（数据一点不动）
sudo ./deploy.sh --start              # 启动服务
sudo ./deploy.sh --logs               # 看最近 200 行应用日志（sudo ./deploy.sh --logs 500 可指定行数）
sudo ./deploy.sh --logs-follow        # 实时跟踪日志（Ctrl+C 退出）
sudo ./deploy.sh --collect            # 【服务起不来也能用】把日志打包到当前目录（发我排查）
sudo ./deploy.sh --uninstall          # 卸载服务（保留数据目录）
sudo ./deploy.sh --check-deps         # 只检查依赖是否已安装（不改动系统，普通用户也可运行）
```

> 部署前想先确认环境？先跑 `sudo ./deploy.sh --check-deps`，会列出
> aria2 / transmission / jq / openssl / yt-dlp / ffmpeg / node 是否已安装，
> 以及「未安装时部署会怎么处理」。

## 1.4 日常运维（更新代码 / 重新部署 / 停止 / 看日志）

### ① 我在电脑上改了代码并推到 GitHub，服务端怎么拉取最新代码并重新部署？

```bash
cd ttdownload-web          # 进工程目录（git clone 下来的那个目录）
sudo ./deploy.sh --update  # 一条命令：git pull + 重装依赖 + 重建前后端 + 重启服务 + 健康检查
```

`--update` 内部做的事（也可以手动一步步来）：

```bash
cd ttdownload-web
git pull --ff-only              # 拉最新代码（有本地改动会失败，先 git stash 或 git checkout -- .）
npm ci && npm run build         # 后端依赖 + TypeScript 构建（dist/）
cd web && npm ci && npm run build && cd ..   # 前端构建（public/）
sudo ./deploy.sh --restart      # 重启服务
sudo ./deploy.sh --logs         # 确认没有报错
```

> 只有前端改动也必须重新构建（`web` 目录 → `public/`），否则页面还是旧的。
> 数据（`.env`、数据库、下载目录、日志）都在 `--update` 中保持不变，放心执行。

### ② 正在运行的服务怎么停止 / 重启 / 看状态？

```bash
sudo ./deploy.sh --stop      # 停止（数据目录、任务记录都不动）
sudo ./deploy.sh --start     # 再启动
sudo ./deploy.sh --restart   # 重启
sudo ./deploy.sh --status    # 服务状态 + /api/health 健康检查
# 等价的原生命令：
sudo systemctl stop|start|restart|status ttdownload-web
```

### ③ 怎么看日志？出问题要发什么给我？

```bash
sudo ./deploy.sh --logs          # 最近 200 行（--logs 1000 看更多）
sudo ./deploy.sh --logs-follow   # 实时滚动
grep 'MARK:TASK_FAIL' /ttdownload/state/app.log        # 只看失败
grep 'MARK:YTDLP'     /ttdownload/state/app.log        # 只看 yt-dlp 相关
```

- 应用日志文件：`/ttdownload/state/app.log`（超过 20MB 自动轮转成 `app.log.1`…最多 5 个）
- 部署脚本日志：`/ttdownload/state/logs/deploy.log`
- systemd 日志：`journalctl -u ttdownload-web -n 200 --no-pager`
- **网页「日志」页**：可视化看日志、按标记/级别/关键字过滤、下载日志文件
- **网页「问题反馈」页（推荐）**：一个页面把所有排查材料列出来，点「下载」就存到本地：
  - **完整诊断报告（推荐）**：一个 zip，内含 README + 全部日志（含轮转）+ 部署日志 + 错误摘要（errors.log）+
    任务清单与失败原因 + 网络自检 + 标记说明；密钥自动打码
  - 也可以只下单项：错误日志摘要、app.log、deploy.log、任务清单(JSON/CSV)、网络自检报告、诊断信息 JSON
  - 页面下方还列出「最近失败的任务」及失败原因（一键复制）
  - 拿到文件直接发我即可；等价命令行：`curl -OJ http://127.0.0.1:8080/api/system/report`
- **服务根本起不来、网页打不开**？用离线收集（不需要服务在跑）：
  ```bash
  cd ttdownload-web && sudo ./deploy.sh --collect
  # 会在当前目录生成 ttdownload-logs-<时间>.zip（含 app.log / 轮转日志 / deploy.log / app.db）
  ```
- 详细排查手册见 [`docs/排查手册.md`](./docs/排查手册.md)

### ④ 改配置

```bash
sudo vim /ttdownload-web/.env && sudo ./deploy.sh --restart
```

### ⑥ 环境问题：用「修复脚本」页远程修复（不用 SSH）

分两类问题：

| 问题类型 | 修复方式 |
| --- | --- |
| **代码逻辑问题** | 我改代码 → push 到 GitHub → 你执行 `sudo ./deploy.sh --update` |
| **系统环境问题**（缺包、权限、systemd 配置、Node 版本…） | 我生成一个修复脚本（`.sh`）发你 → 你在网页「**修复脚本**」页上传 → 一键执行 |

操作步骤：

1. 把我给你的 `.sh` 文件保存到本地（先打开看一眼内容，脚本是纯文本）
2. 打开网页「**修复脚本**」页：
   - 「维护令牌」填 `.env` 里的 `MAINTENANCE_TOKEN`（没设就用 `ANDROID_TOKEN`；服务端未配置令牌时页面会警告）
   - 打开「脚本执行」开关（用完请关掉）
3. 上传脚本 → **核对页面上的脚本预览内容** → 勾选「我已阅读并确认」→ 点「上传并执行」
4. 页面会实时显示执行输出与退出码；跑完点「下载日志」，把日志发我确认结果

> ⚠️ 安全提醒：这个通道是「**上传即以服务身份（root）执行**」，默认关闭，且写操作需要维护令牌。
> **只运行我给你的脚本**，不要运行来路不明的 `.sh`。
> 如果脚本里会重启服务（`systemctl restart ttdownload-web`），页面会短暂打不开——脚本在独立单元里继续执行，等 10 秒刷新即可。
> 建议在 `.env` 里设置 `MAINTENANCE_TOKEN=<一段随机字符串>` 并在用完后把开关关掉。

### ⑤ 日志级别（调试期默认 debug）

`debug` 会记录外部命令的完整 argv、退出码、stdout/stderr 摘要、HTTP 请求、aria2/transmission RPC 调用等，
排查完可以在网页「日志」页把开关关掉（或改 `.env` 的 `LOG_LEVEL=info`）以减小日志体积。

## 1.5 Docker 部署（可选）

```bash
export DOWNLOAD_ROOT=/ttdownload
export ANDROID_TOKEN=$(openssl rand -hex 12)
docker compose up -d
docker compose logs -f
```

---

# 第二部分　Web 管理界面：三大下载模块

浏览器打开 `http://<服务器IP>:8080/`。

## 2.0 页面导航

| 页面 | 作用 |
| --- | --- |
| Dashboard（首页） | 今日任务/完成/下载中/失败、累计下载量、平台分布、每日下载量、成功率、最近任务 |
| 任务 | 下载中 / 等待中 / 已完成 / 失败 分区；暂停、继续、取消、重试、删除 |
| 公开视频下载 | 粘贴视频链接 → 解析 → 选质量格式 → 加入下载队列（详见 2.3） |
| BT 种子 | 上传 zip / 种子列表 / 入队下载（详见 2.1） |
| aria2 | 多行 URL 提交直链下载（详见 2.2） |
| 已发布文件 | 已加密待安卓下载的成品；删除记录 / 删除文件（严格区分） |
| 日志 | 调试开关、按标记/级别/关键字过滤日志、下载日志文件（详见 4.3） |
| 问题反馈 | 一键下载完整诊断报告 / 单项日志 / 任务失败清单 / 网络自检报告（详见 4.3） |
| 修复脚本 | 上传开发者给的环境修复脚本并执行（详见 1.4 ⑥） |
| 设置 | 下载/网络/公开视频(cookies)/BT出清/外观/系统状态/外部工具连通性测试 |

页面右上角显示磁盘可用空间；低于保留阈值时会给出警告提示。

## 2.1 transmission（BT 种子）模块

**准备 zip 包**：把任意数量 `.torrent` 种子文件打成一个 zip。

1. 打开「BT 种子」页面 → 上传该 zip（或直接把 zip 放进
   `/ttdownload/transmission/btzhongzi_zip/`，系统每几分钟自动扫描）
2. 系统自动解压 → 种子进入待下载列表（页面下方可见）
3. 勾选种子 → 「加入下载队列」
4. 系统行为：
   - 先读取种子文件清单，**只勾选视频与图片**（其它文件不下载）
   - 计算这些文件总大小，交给统一队列做磁盘门控（保证下载完仍有 ≥10GiB 空闲）
   - 下载完成后：多文件自动打 zip、单文件原样，统一命名归档 → 加密 → 发布
   - 同时删除 transmission 任务与其下载目录文件

### 2.1.1 BT 出清机制（自动清理"永远下不完"的任务）

BT 常有三种"永远下不完"的情况，系统会自动出清（删除任务+清理文件，释放空间）：

| 情况 | 判定 |
| --- | --- |
| ① 完全无资源 | 速率为 0、无 peer、进度停滞 ≥30 分钟 |
| ② 中途停滞 | 先能下、后来长期卡在某个百分比不动（≥30 分钟无进度） |
| ③ 还有资源但极慢 | 速率 < 20KB/s 且预计剩余 > 72 小时 |

**硬门槛（防误删）**：只有**实际下载尝试满 10 小时**的任务才参与判断；
因磁盘空间不足被自动暂停的时间不计入，手动暂停的任务也不判断。

**进度 ≥79% 的视频**：按"未下完但可播放（如 mp4 用 VLC 也能播）"处理 →
**移交归档 → 加密 → 发布**，不删除；发布完成后再清理残留目录。

删除时同时清理：transmission 任务 + 我们自己的下载目录 +
transmission 的 incomplete 目录（默认 `/var/lib/transmission/incomplete`）下对应任务文件夹。

每次出清后都会**广播"空间已腾挪"**（页面右上角会弹提示），等待队列**立即**重新评估并放行后续任务——
不管这次只腾挪了几百 MB 还是几十 GB，只要释放了就会通知。

操作位置：
- 「BT 种子」页顶部有 **出清预览**（只查看不修改）与 **立即出清**（马上执行一次）；
- 「设置」页 → **BT 出清**：调整 10 小时门槛、停滞时长、极慢阈值、检查周期、可播放进度阈值等；
- API：`GET /api/bt/stale`（预览）、`POST /api/bt/evict`（执行）。

> 提示：升级到本版本后，"实际下载尝试时间"从 0 开始累计（保守策略，宁可晚清理也不误删）；
> 对已经挂了很久、你确定不要的任务，可以直接在「任务」页取消，或点「立即出清」，
> 或用 `POST /api/bt/evict` 触发检查。

## 2.2 aria2（URL 直链）模块

1. 打开「aria2」页面，在文本框粘贴 URL，**一行一个**（支持多行批量）
2. 点「加入下载队列」；系统会逐条校验（非 http(s) 会给出原因）并自动去重
3. 任务在「任务」页面查看进度/速度/剩余时间，支持暂停/继续/取消/重试
4. 判定完成的规则：aria2 任务状态 complete **且**同目录没有 `.aria2` 控制文件
5. 完成后单文件直接归档（aria2 任务都是单文件，不打包）

## 2.3 公开视频URL 模块（yt-dlp）

支持平台：YouTube、Bilibili、Vimeo、X、TikTok、Instagram、抖音 等（以 yt-dlp 实际支持为准）。

1. 首页大输入框粘贴视频链接（也可以把链接拖进输入区）
2. 点「解析视频」→ 展示预览卡片：标题、缩略图、平台、时长、作者、可用分辨率与格式、文件大小
3. 选择质量与格式 → 点「加入下载队列」
4. 任务列表实时显示百分比、进度条、速度、剩余时间，支持暂停/继续/取消/重试/删除
5. 失败会显示可读中文原因，并告诉你**下一步该怎么办**（例如会员专享 → 去上传 cookies）

### 2.3.1 「想尽办法」的下载策略（会员 / 需登录 / 年龄限制都不再直接放弃）

任务启动后，程序会**自动按顺序换着方式试**，任何一种成功就完成任务；只有全部失败才判失败：

| 顺序 | 方式 | 说明 |
|---|---|---|
| 1 | 指定格式（你选的清晰度） | 解析成功时会带上这一档 |
| 2 | 登录态 + 最佳画质 | 配了 cookies 才有这一档 |
| 3 | 登录态 + 多客户端 | 配了 cookies + YouTube |
| 4 | 最佳画质 | `bv*+ba/b` 自动选最好 |
| 5 | 多客户端回退 | YouTube 逐个换 player_client |
| 6 | 长重试 + 放宽校验 | retries/fragment-retries 拉高、`--geo-bypass`、忽略证书 |
| 7 | 模拟浏览器指纹 | `--impersonate chrome` |
| 8 | 内嵌播放器客户端 | 换 web_embedded |
| 9 | 单文件直下（跳过合并） | 避免 ffmpeg 合并失败 |
| 10 | 仅视频流（可能无音轨） | 先保证有文件 |
| 11 | 仅音频（保底） | 最后一招 |

另外：**解析失败也不再直接拒绝**。遇到会员专享/需登录导致解析不出信息时，页面会显示「解析受限（仍可下载）」的橙色提示和「仍然下载（自动多方式尝试）」按钮，你照样可以把它丢进队列去试。

失败后的自动重试规则：会员/登录/私有这类"以后可能能下"的错误，会走满「设置 → 自动重试次数」才最终判失败（期间你可以随时补 cookies 再点重试）；只有真正无解的错误（DRM 保护、URL 格式错误、平台不支持）才立即失败。

### 2.3.2 用 cookies 解决会员专享 / 需登录 / 年龄限制视频

报错形如：

```
下载失败：[youtube] f6kl3G_ek-A: This video is available to this channel's members on level:
高级VIP会员（人工咨询服务） (or any higher level). Join this channel to get access to members-only content...
```

这就是**频道会员专享**：只有该频道的会员账号（或更高等级）才能观看。程序无法凭空获得会员权限，但如果**你本人就是会员**，把自己的登录态（cookies）交给它，就能正常下载：

1. 在能正常观看该视频的浏览器里登录该网站（比如 YouTube 会员账号）
2. 用浏览器扩展导出 cookies，推荐 **Get cookies.txt LOCALLY**（Chrome/Edge/Firefox 均有），导出为 `cookies.txt`（Netscape 格式）
3. 打开网页「**设置 → 公开视频（yt-dlp）**」：
   - 点「**上传 cookies.txt**」直接上传（保存到 `DOWNLOAD_ROOT/state/cookies.txt`，权限 600）；或
   - 把已有的 cookies.txt 放到服务器上，并在「cookies 文件路径」里填它的绝对路径
   - 若服务器上就装了浏览器，也可以选「从浏览器读取 cookies」（chrome/firefox/...）
4. 保存设置 → 回任务页点「重试」（或重新解析加入队列）

> 会员专享、年龄限制、人机校验（"Sign in to confirm you're not a bot"）、私有视频但你有权限 —— 这些情况都是靠 cookies 解决。
> 如果连 cookies 都没有、也不是会员，那就是**真的没有招**了，程序会在试完全部方式后如实告诉你。

其他可选：卡片里的「额外参数」会追加到每次 yt-dlp 调用，常见用法是走代理：
`--proxy socks5://127.0.0.1:1080`，或换 UA：`--user-agent "Mozilla/5.0 ..."`。

> 合规说明：本工具只处理你有权访问/下载的资源；cookies 是**你自己账号的登录态**，用于访问你自己有权看的内容；**不实现 DRM 破解、不绕过付费墙**。

### 2.3.3 首页「网络自检」：确认这台机器能不能真的下载视频

首页输入框下方有一块「**网络自检**」面板，点开就知道服务器（比如树莓派）到 YouTube 这条链路到底通不通。
它会**真的出网测试**，不是只看端口：

| 分组 | 检查项 | 说明 |
| --- | --- | --- |
| 基础网络 | 代理配置 | 显示 `HTTP(S)_PROXY` 环境变量与 yt-dlp 的额外参数（有没配代理一眼看到） |
| 基础网络 | DNS 解析 | 解析 `www.youtube.com` / `redirector.googlevideo.com` / `github.com` |
| 基础网络 | HTTPS 直连 Google / YouTube / GitHub | 三个独立探测，分别给出耗时；哪个不通一目了然 |
| yt-dlp / YouTube | yt-dlp 可执行 | 显示实际版本号 |
| yt-dlp / YouTube | yt-dlp 解析 YouTube 元数据 | **关键项**：用 yt-dlp 真去解析一个公开测试视频（会带上你设置的 cookies/代理） |
| yt-dlp / YouTube | 视频 CDN（googlevideo）可达 | 拿到视频直链后真去读 1 字节：**能过这关才说明真的下载得动** |
| 本机下载服务 | aria2 RPC / transmission RPC | 本机下载引擎是否就绪（BT 与 URL 直链模块依赖） |

- 顶部徽标：绿色「网络正常」/ 黄色「部分可用」/ 红色「网络异常」+ 一句话结论
- 失败项会给出**具体建议**（例如「googlevideo 连不上：给 yt-dlp 配 `--proxy`，或检查 OpenWrt 的分流规则」）
- 结果缓存 60 秒，点「重新检测」可强制重测（故障排查时的第一件事）
- 常见结论对照：
  - 基础网络全绿、YouTube 元数据失败 → 出口访问不了 YouTube：在「设置 → 公开视频（yt-dlp）」填 `--proxy socks5://...`
  - 元数据成功但 CDN 失败 → 拿到地址了但下载通道被拦：多半是代理/分流没覆盖 `googlevideo.com`
  - aria2/transmission RPC 失败 → 本机引擎没装好，跟外网无关（按提示装/填密码）

## 2.4 归档 / 加密 / 发布（自动，无需操作）

任何一个模块下载完成 → 自动进入流水线：

```
下载完成 → downd_ok_p2（打包/命名/V-L-T 标记）
        → downd_ok_p2_jiami_tmp（AES-256-CBC 加密）
        → 去掉 .data 后缀
        → xiaofeizhe_downd（消费者目录）→ 记录到数据库，出现在「已发布文件」页面
```

- 加密参数与老脚本完全一致：`key = sha256(密码)`、`iv = md5(密码)`、算法 `aes-256-cbc`
- 文件名规则：`3 个小写字母前缀 + 递增序号`（如 `oqq1`），序号文件 `/ttdownload/state/indexFXY`
- 文件尾部带 `FKY996`（V-L-T）标记，记录**原始文件名**，PC 端解密后可还原

## 2.5 设置页

- **下载设置**：默认质量、默认格式、最大并发（1/2/3/5/10）、默认保存目录
- **网络设置**：最大下载速度、请求超时、自动重试次数
- **外观**：Light / Dark / System
- **系统**：版本、数据库状态、下载目录占用、磁盘剩余、外部工具（aria2/transmission/yt-dlp/ffmpeg/openssl）是否可用，并可一键测试连通性

---

# 第三部分　安卓 App：消费端使用

## 3.1 安装与首次配置

1. 安装 APK：`AriaNgGUI-AutoDL-v1.3.2-debug.apk`
2. 首次启动授予**存储权限**（用于下载与写日志）
3. 打开左侧边栏 →「自动下载列表」
4. 在页面顶部「🌐 新服务端 API（ttdownload-web）」区域填写：
   - **服务器地址**：`http://<服务器IP>:8080`（deploy 脚本打印的地址）
   - **安卓 Token**：部署时生成（见服务器 `.env` 的 `ANDROID_TOKEN`）
   - 点「保存 API 配置」（只需配置一次，所有服务器共用）
5. 在「下载列表服务器」区域点「＋ 添加服务器」：
   - 名称随意（如 `myServer`）
   - **列表URL 留空**（使用 API 模式时不需要填写）
   - 扫描间隔建议 5 分钟
   - 保持启用

## 3.2 工作流程

```
App 每 N 分钟 → GET /api/android/files 拉取待下载清单
     ↓ 对每个文件：本地去重（已下载记录 / 已创建清单 / aria2 队列 / 文件名）
     ↓ 交给 App 内置 aria2 下载（支持断点续传）
下载完成 → POST /api/android/done {ids:[...]} 上报
     ↓ 服务端删除该文件（腾出空间）→ 调度器自动放行下一个等待任务
     ↓ App 同时写入本地“已下载记录”，此后不再重复下载
```

- **本地去重三件套**（都在手机 Download 目录，可查看/删除）：
  - `aria2.created.txt`：已创建过下载任务的清单（命中绝不重复创建）
  - `aria2.downloaded.txt`：已下载完成清单
  - `aria2-autodl.log`：运行日志（排障用）
- **上报失败自动重试**：断网等失败会进入持久化待上报队列，指数退避重试（5 分钟起，最长 2 小时），App 重启不丢
- **SD 卡存储（可选）**：面板底部「SD 卡存储」→ 选择一次 SD 目录 → 勾选启用；下载完成后自动搬进 SD 卡并删除内置副本；拔卡/换卡/空间不足时文件安全保留在内置目录并自动重试

## 3.3 老模式兼容（nginx dowlist）

如果暂时还在用老脚本 `video_auto.sh`（nginx 静态清单）：
- 「🌐 新服务端 API」留空；
- 在每个服务器的**列表URL**里填 `http://<服务器IP>/dowlist`；
- 上报地址会按老规则自动推导为 `http://<服务器IP>/autodl/done`（无需配置）。

两种模式可以共存：配了 API 的服务器走 API，填了 dowlist 的服务器走老模式。

---

# 第四部分　联调验证与常见问题

## 4.1 端到端验证清单（建议照做一遍）

```bash
# ① 服务端健康
curl -s http://<IP>:8080/api/health

# ② 提交一个 aria2 直链任务（举例）
curl -s -X POST http://<IP>:8080/api/aria2/urls \
  -H 'Content-Type: application/json' \
  -d '{"urls":"http://<某个可下载文件URL>"}'

# ③ 看任务与统计
curl -s http://<IP>:8080/api/tasks | head -c 400
curl -s http://<IP>:8080/api/stats | head -c 400

# ④ 等它下载-归档-加密-发布完成（页面“已发布文件”会出现新文件）

# ⑤ 用安卓 Token 拉清单
curl -s -H 'X-Auth-Token: <你的Token>' http://<IP>:8080/api/android/files

# ⑥ 模拟安卓上报完成（把 id 换成上一步返回的 id）
curl -s -X POST -H 'X-Auth-Token: <你的Token>' -H 'Content-Type: application/json' \
  -d '{"ids":[1]}' http://<IP>:8080/api/android/done

# ⑦ 确认文件已被删除
ls /ttdownload/xiaofeizhe_downd/
```

## 4.2 常见问题

| 现象 | 原因 / 处理 |
| --- | --- |
| 页面打不开 | `systemctl status ttdownload-web`；确认端口已放行（`ufw allow 8080`） |
| 页面 404 或显示后端提示页 | 前端未构建：`npm run build:web` 后 `systemctl restart ttdownload-web` |
| 任务一直「等待中」 | 磁盘可用空间低于保留值；清理 `xiaofeizhe_downd` 或调小 `RESERVE_FREE_BYTES` |
| yt-dlp 解析失败 | 服务器上执行 `yt-dlp -U` 升级；设置页可测试连通性 |
| BT 任务失败 | 确认 transmission-daemon 在运行：`systemctl status transmission-daemon`；并用 `sudo ./deploy.sh --check-deps` 看依赖 |
| transmission 未安装/装坏了 | 运行 `sudo bash deploy/ubuntutr.sh`（交互脚本，会问白名单与密码），完成后 `sudo ./deploy.sh --restart` |
| BT 报 RPC 401/认证失败 | transmission 安装脚本默认要求密码认证：在网页「设置 → transmission RPC」填 用户名 `opengl` + 你设置的密码（或写入 `.env` 的 `TRANSMISSION_RPC_USER/PASSWORD` 后重启服务） |
| 安卓 App 报 401 | Token 不一致；见服务器 `.env` 的 `ANDROID_TOKEN` |
| 安卓 App 拉不到清单 | 服务器防火墙未放行端口；或 App 里服务器地址写错（不要漏 `http://` 和端口） |
| 安卓下载的密文如何解密 | 用 `ENCRYPT_PASSWORD` 对应的 key/iv（`sha256(密码)` / `md5(密码)`）解密，再按文件尾 `FKY996` 标记还原原始文件名（兼容老脚本 `all1.sh`） |
| BT 任务一直停在某个百分比 | 等出清机制处理（≥10 小时尝试 + 停滞 30 分钟）；急的话用「BT 种子」页「立即出清」或在任务页取消 |
| 找不到 transmission 的 incomplete 目录 | 默认 `/var/lib/transmission/incomplete`；若你的 daemon 配置了别的路径，改 `.env` 的 `TRANSMISSION_INCOMPLETE_DIR` |
| 想重新下载已下载过的文件 | 删除手机 `Download/aria2.downloaded.txt` 与 `Download/aria2.created.txt`（或面板里的「清空」按钮） |
| 在线视频下载不了 | 先看首页「网络自检」面板（2.3.3）：哪一项红就按它的建议修；会员/登录类视频按 2.3.2 传 cookies |
| 更新代码后页面没变化 | 只 pull 没重新构建：`sudo ./deploy.sh --update`（会重建前端 `public/`） |
| 想停止但保留数据 | `sudo ./deploy.sh --stop`（数据目录 `/ttdownload` 完全不动） |

> 遇到上面表里没有的问题：**网页「日志」页 → 导出诊断包**，把文件 + 报错信息发我，见下一节。

## 4.3 排查问题：日志、标记与诊断包

> **最省事的方式**：打开网页「**问题反馈**」页 → 点「**一键下载诊断报告**」→ 把下载到的
> `ttdownload-report-*.zip` 连同报错截图发我。里面已经包含了下面要 grep 的所有内容。

调试期所有关键动作都打了**标记**（形如 `[MARK:XXX]`），可以直接 grep 定位：

```bash
grep 'MARK:TASK_FAIL'  /ttdownload/state/app.log   # 任务为什么失败
grep 'MARK:YTDLP'      /ttdownload/state/app.log   # yt-dlp 解析/下载每一步（含完整命令行与 stderr）
grep 'MARK:TR_RPC'     /ttdownload/state/app.log   # transmission RPC（含 401/409 认证与协商问题）
grep 'MARK:ARIA2_RPC'  /ttdownload/state/app.log   # aria2 RPC 调用与错误
grep 'MARK:DISK_GATE'  /ttdownload/state/app.log   # 任务为什么一直等待（空间门控数值）
grep 'MARK:BT_EVICT'   /ttdownload/state/app.log   # BT 出清判定细节
grep 'MARK:NET_CHECK'  /ttdownload/state/app.log   # 网络自检每一项结果
grep 'MARK:PROC_EXIT'  /ttdownload/state/app.log   # 外部命令非零退出
```

完整标记清单在网页「日志」页底部（标记 / 含义对照表），也可以看
[`docs/排查手册.md`](./docs/排查手册.md)（含「发什么给我最有帮助」清单）。

## 4.4 数据与备份

- 数据库：`/ttdownload/state/app.db`（SQLite，任务/发布文件/设置）
- 日志：`/ttdownload/state/app.log`
- 待发布成品：`/ttdownload/xiaofeizhe_downd/`
- 建议对 `state/` 做定期备份；`xiaofeizhe_downd/` 属于"下完即删"的中转目录，不必备份。
